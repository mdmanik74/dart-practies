
void main()
{
    int a = 5;
    int b = 7;
 
    // Conditional Statement
    var c = (a < 10) ? 
    print(c);
 
    // Conditional statement
    int n;
    var d = n ? ? 
    print(d);
 
    // After assigning value to n
    n = 10;
    d = n ? ? 
    print(d);
}